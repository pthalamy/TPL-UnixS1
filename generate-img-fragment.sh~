#! /bin/sh

usage () {
    echo "Veuillez entrer le chemin vers l'image en argument."
}

if [ $# -eq 1 ] && [ -d $1 ]; then
    cd $1
    for I in $(ls); do
	if [ -d $I ]; then
	    echo $I
	fi
    done
else
    usage
fi